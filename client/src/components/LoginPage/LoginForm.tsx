const LoginForm = () => {
    const login = () => {};

    return (
        <form onSubmit={login} className="p-6 rounded-2xl flex flex-col">
            <div>hello</div>
        </form>
    );
};

export default LoginForm;
