import LoginForm from "../components/LoginPage/LoginForm";

const LoginPage = () => {
    return (
        <div className="w-full h-[calc(100vh-3.75rem)] flex justify-center items-center">
            <LoginForm />
        </div>
    );
};

export default LoginPage;
